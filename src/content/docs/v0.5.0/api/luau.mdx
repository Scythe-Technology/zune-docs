{/* This file was auto-generated with "zune-docs" */}

# Luau
`zune.luau`

---
import { Callout, Tabs } from 'nextra/components'

A library to parse, compile and run luau code.

## Functions
### compile
Compiles `Luau` source code.
```luau tooltips
-- @hover: (40,58)[export type LuauCompileOptions = {
    debug_level: number?,
    optimization_level: number?,
    coverage_level: number?, 
}]
compile(source: string | buffer, opts: LuauCompileOptions?): buffer
```
<details open>
<summary>Parameters</summary>
- `source: string | buffer{:luau}` - The source code to compile.
- `opts: LuauCompileOptions?{:luau}` - The options for the compiler.
  - `debug_level: number?{:luau}` - The debug level for the compilation.
  - `optimization_level: number?{:luau}` - The optimization level for the compilation.
  - `coverage_level: number?{:luau}` - The coverage level for the compilation.
</details>
<details>
<summary>Throws</summary>
- Luau Error
- Memory Error
</details>

### load
Loads `Luau` bytecode into a function.
```luau tooltips
-- @hover: (39,54)[export type LuauLoadOptions = {
    env: {
        [any]: any
    }?,
    chunk_name: string?,
    native_code_gen: boolean?, 
}]
load(bytecode: string | buffer, opts: LuauLoadOptions?): ((...any) -> ...any)
```
<details open>
<summary>Parameters</summary>
- `bytecode: string | buffer{:luau}` - The bytecode to load.
- `opts: LuauLoadOptions?{:luau}` - The options for loading the bytecode.
  - `env: {[any]: any}?{:luau}` - The environment to use for the loaded function.
  - `chunk_name: string?{:luau}` - The name of the chunk, used for debugging.
    - default: `"(load)"{:luau}`
  - `native_code_gen: boolean?{:luau}` - Whether to enable native code generation.
    - default: `true{:luau}`
</details>
<details>
<summary>Throws</summary>
- Luau Error
</details>

### coverage
Get coverage data from a function.
```luau tooltips
-- @hover: (36,52)[export type LuauCoverageData = {
    name: string,
    line: number,
    depth: number,
    [number]: number, 
}]
coverage(fn: (...any) -> ...any): {LuauCoverageData}
```
<details open>
<summary>Parameters</summary>
- `fn: (...any) -> ...any{:luau}` - The function to get the coverage data from.
</details>

### parse
Parses `Luau` source code.
```luau tooltips
-- @hover: (30,41)[export type Location = {
    begin: Position,
    ["end"]: Position, 
}

type Position = {
    line: number,
    column: number, 
}

export type ParseResult = {
    errors: {{
        message: string,
        location: Location, 
    }}?,
    root: {any},
    eof: {
        text: "",
        position: Position,
        tag: "eof"
    },
    lines: number,
    lineOffsets: {number},
    hotcomments: {{
        header: boolean,
        content: string,
        location: Location, 
    }}, 
}]
parse(src: string | buffer): ParseResult
```
<details open>
<summary>Parameters</summary>
- `src: string | buffer{:luau}` - The source code to parse.
</details>
<details>
<summary>Throws</summary>
- Memory Error
</details>

### parseExpr
Parses a `Luau` expression.
```luau tooltips
parseExpr(src: string | buffer): any
```
<details open>
<summary>Parameters</summary>
- `src: string | buffer{:luau}` - The expression to parse.
</details>
<details>
<summary>Throws</summary>
- Memory Error
</details>

### garbagecollect
Runs a full garbage collection cycle.
```luau tooltips
garbagecollect(): ()
```
